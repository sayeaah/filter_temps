{
  "name": "sample",
  "version": "1.0.0",
  "scripts": {
    "compileSass": "sass --watch src/scss:assets/css --no-source-map",
    "compilePug": "pug -w src/pug -o ./ -P",
    "start": "npm-run-all --parallel compileSass compilePug"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
